package com.lowes.auditor.client

import io.kotest.core.spec.style.BehaviorSpec

/**
 * Integration class that needs to be extended for all integration tests
 */
open class IntegrationTestSpec : BehaviorSpec()
